{
  "name": "secure-form-filler",
  "version": "1.0.0",
  "description": "Secure form filler",
  "scripts": {
    "build": "node encryptor.js",
    "deploy": "gh-pages -d public",
    "dev": "npm run build && chokidar 'src/**/*.js' -c 'npm run build'"
  },
  "dependencies": {
    "xlsx": "^0.18.5",
    "crypto-js": "^4.1.1"
  },
  "devDependencies": {
    "cpx": "^1.5.0",
    "gh-pages": "^5.0.0",
    "rimraf": "^5.0.1",
    "chokidar-cli": "^3.0.0"
  }
}
